# 管理员模块流程图和类图

## 图片格式说明

本文档包含的流程图和类图使用Mermaid语法编写，可以通过以下方式生成图片：

### 方法1：在线生成图片
1. 访问 [Mermaid Live Editor](https://mermaid.live/)
2. 复制下面的Mermaid代码到编辑器
3. 点击"Download PNG"或"Download SVG"下载图片

### 方法2：使用VS Code插件
1. 安装"Mermaid Preview"插件
2. 在VS Code中打开此文件
3. 使用Ctrl+Shift+P，搜索"Mermaid: Preview"
4. 右键点击预览图，选择"Save as Image"

### 方法3：使用命令行工具
```bash
npm install -g @mermaid-js/mermaid-cli
mmdc -i input.md -o output.png
```

---

## 1. 导入学生/教师信息模块

### 流程图
```mermaid
flowchart TD
    A[管理员登录] --> B[选择导入功能]
    B --> C[选择导入类型]
    C --> D{导入类型}
    D -->|学生信息| E[下载学生信息模板]
    D -->|教师信息| F[下载教师信息模板]
    D -->|用户信息| G[下载用户信息模板]
    E --> H[填写Excel文件]
    F --> H
    G --> H
    H --> I[上传Excel文件]
    I --> J[系统验证文件格式]
    J --> K{验证结果}
    K -->|成功| L[解析Excel数据]
    K -->|失败| M[显示错误信息]
    M --> I
    L --> N[批量导入数据库]
    N --> O[显示导入结果]
```

### 类图
```mermaid
classDiagram
    class AdminController {
        +importExcel(file: MultipartFile): ResponseEntity
        +downloadTemplate(type: String): ResponseEntity
    }
    
    class AdminService {
        +importExcel(file: MultipartFile): ImportResult
        +importData(dataList: List, type: String): void
        +validateExcelFormat(file: MultipartFile): boolean
    }
    
    class ImportExcelForm {
        +file: File
        +importType: String
        +uploadFile(): void
        +downloadTemplate(): void
    }
    
    class StudentDAO {
        +batchInsert(students: List): void
        +findByStudentId(id: String): Student
    }
    
    class TeacherDAO {
        +batchInsert(teachers: List): void
        +findByTeacherId(id: String): Teacher
    }
    
    AdminController --> AdminService
    AdminService --> StudentDAO
    AdminService --> TeacherDAO
    ImportExcelForm --> AdminController
```

## 2. 学生分配模块

### 流程图
```mermaid
flowchart TD
    A[管理员进入分配页面] --> B[选择班级]
    B --> C[查看学生列表]
    C --> D[查看可用导师列表]
    D --> E[选择学生]
    E --> F[分配学术导师]
    F --> G[分配企业导师]
    G --> H[确认分配]
    H --> I[保存分配关系]
    I --> J[更新学生信息]
    J --> K[显示分配结果]
```

### 类图
```mermaid
classDiagram
    class StudentController {
        +getStudentsByClass(className: String): List
        +assignAdvisor(studentId: String, advisorId: String): ResponseEntity
    }
    
    class TeacherController {
        +getAvailableAdvisors(): List
        +getTeacherById(id: String): Teacher
    }
    
    class SelectRelationshipForm {
        +selectedClass: String
        +studentList: List
        +advisorList: List
        +assignAdvisor(): void
        +filterByClass(): void
    }
    
    class StudentService {
        +assignAcademicAdvisor(studentId: String, advisorId: String): void
        +assignIndustryAdvisor(studentId: String, advisorId: String): void
        +getStudentsByClass(className: String): List
    }
    
    class TeacherService {
        +getAvailableAdvisors(): List
        +updateAdvisorAssignment(advisorId: String): void
    }
    
    SelectRelationshipForm --> StudentController
    SelectRelationshipForm --> TeacherController
    StudentController --> StudentService
    TeacherController --> TeacherService
```

## 3. 企业信息审核模块

### 流程图
```mermaid
flowchart TD
    A[管理员查看实习信息] --> B[筛选待审核信息]
    B --> C[查看学生实习详情]
    C --> D[检查企业信息]
    D --> E[验证实习岗位]
    E --> F[核实联系方式]
    F --> G{审核决定}
    G -->|通过| H[更新审核状态为通过]
    G -->|拒绝| I[更新审核状态为拒绝]
    G -->|需要修改| J[标记需要修改]
    H --> K[发送通知]
    I --> K
    J --> K
    K --> L[记录审核日志]
```

### 类图
```mermaid
classDiagram
    class InternshipController {
        +getInternshipList(): List
        +reviewInternship(id: String, status: String): ResponseEntity
        +getInternshipDetails(id: String): InternshipInfo
    }
    
    class InternshipService {
        +getAllInternships(): List
        +updateReviewStatus(id: String, status: String): void
        +validateInternshipInfo(info: InternshipInfo): boolean
    }
    
    class ViewStudentInfoBase {
        +internshipList: List
        +filterStatus: String
        +loadInternshipData(): void
        +reviewInternship(): void
    }
    
    class InternshipDAO {
        +findAll(): List
        +updateStatus(id: String, status: String): void
        +findByStatus(status: String): List
    }
    
    ViewStudentInfoBase --> InternshipController
    InternshipController --> InternshipService
    InternshipService --> InternshipDAO
```

## 4. 学生成绩查看模块

### 流程图
```mermaid
flowchart TD
    A[管理员进入成绩页面] --> B[选择查看范围]
    B --> C{查看类型}
    C -->|按班级| D[选择班级]
    C -->|按学生| E[搜索学生]
    C -->|全部| F[加载所有成绩]
    D --> G[加载班级成绩]
    E --> H[加载学生成绩]
    F --> I[显示成绩列表]
    G --> I
    H --> I
    I --> J[查看详细评分]
    J --> K[导出成绩报告]
```

### 类图
```mermaid
classDiagram
    class AssessmentController {
        +getStudentAssessments(): List
        +getAssessmentByClass(className: String): List
        +exportAssessmentReport(): ResponseEntity
    }
    
    class AssessmentService {
        +getAllAssessments(): List
        +getAssessmentsByClass(className: String): List
        +calculateTotalScore(assessment: Assessment): double
    }
    
    class ViewStudentAssessment {
        +assessmentList: List
        +selectedClass: String
        +loadAssessmentData(): void
        +exportReport(): void
    }
    
    class AssessmentDAO {
        +findAll(): List
        +findByClass(className: String): List
        +findByStudentId(studentId: String): Assessment
    }
    
    ViewStudentAssessment --> AssessmentController
    AssessmentController --> AssessmentService
    AssessmentService --> AssessmentDAO
```

## 5. 学生周记管理模块

### 流程图
```mermaid
flowchart TD
    A[管理员进入周记管理] --> B[选择查看周次]
    B --> C[选择班级筛选]
    C --> D[选择完成状态]
    D --> E[加载周记列表]
    E --> F[查看学生周记]
    F --> G{周记状态}
    G -->|已完成| H[查看周记内容]
    G -->|未完成| I[查看未提交学生]
    H --> J[评价周记质量]
    I --> K[发送提醒通知]
    J --> L[记录评价结果]
    K --> L
```

### 类图
```mermaid
classDiagram
    class DiaryController {
        +getDiariesByWeek(week: int): List
        +getDiariesByClass(className: String): List
        +reviewDiary(diaryId: String, comment: String): ResponseEntity
    }
    
    class DiaryService {
        +getDiariesByWeekAndClass(week: int, className: String): List
        +getCompletionStatus(week: int, className: String): Map
        +addReviewComment(diaryId: String, comment: String): void
    }
    
    class ViewStudentInfoDiary {
        +selectedWeek: int
        +selectedClass: String
        +completionStatus: String
        +diaryList: List
        +loadDiaryData(): void
        +reviewDiary(): void
    }
    
    class DiaryDAO {
        +findByWeekAndClass(week: int, className: String): List
        +updateReviewComment(diaryId: String, comment: String): void
        +getCompletionStatistics(week: int): Map
    }
    
    ViewStudentInfoDiary --> DiaryController
    DiaryController --> DiaryService
    DiaryService --> DiaryDAO
```

## 6. 报告下载模块

### 流程图
```mermaid
flowchart TD
    A[管理员选择下载功能] --> B{下载类型}
    B -->|单个学生| C[输入学生ID]
    B -->|批量下载| D[选择下载范围]
    C --> E[生成学生Word文档]
    D --> F[批量生成Word文档]
    E --> G[下载单个文档]
    F --> H[打包为ZIP文件]
    H --> I[下载ZIP文件]
    G --> J[完成下载]
    I --> J
```

### 类图
```mermaid
classDiagram
    class AdminController {
        +generateSingleReport(studentId: String): ResponseEntity
        +generateBatchReports(): ResponseEntity
    }
    
    class StudentWordDataUtil {
        +generateStudentWordDocument(studentId: String): File
        +generateBatchWordDocuments(): File
        +createWordTemplate(): Document
    }
    
    class TeacherService {
        +downloadStudentsReportsZip(): File
        +getStudentReportData(studentId: String): StudentReportData
    }
    
    class ReportDownloadComponent {
        +selectedStudentId: String
        +downloadType: String
        +downloadSingleReport(): void
        +downloadBatchReports(): void
    }
    
    ReportDownloadComponent --> AdminController
    AdminController --> StudentWordDataUtil
    AdminController --> TeacherService
```

## 总体架构图

```mermaid
classDiagram
    class AdminHome {
        +currentComponent: String
        +navigationItems: List
        +switchComponent(): void
    }
    
    class AdminController {
        +importExcel(): ResponseEntity
        +generateReports(): ResponseEntity
        +downloadTemplate(): ResponseEntity
    }
    
    class AdminService {
        +importExcel(): ImportResult
        +validateData(): boolean
        +processImport(): void
    }
    
    AdminHome --> ImportExcelForm
    AdminHome --> SelectRelationshipForm
    AdminHome --> ViewStudentInfoBase
    AdminHome --> ViewStudentAssessment
    AdminHome --> ViewStudentInfoDiary
    
    ImportExcelForm --> AdminController
    SelectRelationshipForm --> StudentController
    ViewStudentInfoBase --> InternshipController
    ViewStudentAssessment --> AssessmentController
    ViewStudentInfoDiary --> DiaryController
    
    AdminController --> AdminService
    StudentController --> StudentService
    InternshipController --> InternshipService
    AssessmentController --> AssessmentService
    DiaryController --> DiaryService
```